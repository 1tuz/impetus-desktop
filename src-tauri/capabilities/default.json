{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability set for Impetus Desktop (IPC via Rust only)",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
